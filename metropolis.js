!(function () {
  const seed = 10001; // make reproducible
  let n; // iterations
  let r; // neighborhood
  let i = 3; // number of initial points
  const f = 500; // number of tween frames
  const d = 2 * Math.PI / 180; // angle direction noise
  const p_branch = .1; // probability of branching
  const w = .8 * innerWidth;
  const h = .8 * innerHeight;
})();